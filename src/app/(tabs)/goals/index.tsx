import { GoalsScreen } from "@/screens";

/**
 * Render the Goals screen.
 *
 * @returns A React element that renders the `GoalsScreen` component
 */
export default function GoalsRoute() {
  return <GoalsScreen />;
}
